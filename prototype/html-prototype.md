# HTML 프로토타입 프롬프트

## 기본 HTML 프로토타입 생성
다음 기능의 HTML 프로토타입을 만들어줘.
기능명: [기능명]

대상 사용자: [사용자 유형]

핵심 시나리오: [사용자가 이 화면에서 하려는 것]
요구사항:

[요구사항 1]
[요구사항 2]

조건:

외부 라이브러리 없이 순수 HTML/CSS/JS로 작성
실제 동작하는 인터랙션 포함 (클릭, 입력 등)
모바일/데스크탑 반응형
더미 데이터로 실제처럼 보이게


## 화면 흐름 프로토타입 (멀티 스텝)
[기능명]의 전체 사용자 흐름을 HTML로 만들어줘.
화면 순서:





각 화면은 버튼 클릭으로 다음 화면으로 넘어가게 해줘.

상단에 현재 단계 표시(progress bar)도 추가해줘.

## 프로토타입 개선
위 프로토타입에서 다음을 개선해줘:

[개선 요청 1]
[개선 요청 2]

변경한 부분을 코멘트로 표시해줘.

